# Sometimes we want to interate over a certain number of elements 
# to do so, we can use in-build "range()" function
# The syntax: range(start, stop, step)

for index in range(0, 100, 3):
    print(index)